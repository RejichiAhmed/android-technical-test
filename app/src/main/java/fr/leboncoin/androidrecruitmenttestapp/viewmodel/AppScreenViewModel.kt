package fr.leboncoin.androidrecruitmenttestapp.viewmodel

import androidx.lifecycle.ViewModel

class AppScreenViewModel : ViewModel() {
    // Minimal app-level ViewModel; delegates to feature modules
    // Can be extended for app-level UI state if needed
}
